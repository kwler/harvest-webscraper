import { expect } from 'chai';
import 'mocha';

describe('index', () => {

    it('should return hello world', () => {
        const expected = 'test';
        expect(expected).to.equal('test');
    });
    
});