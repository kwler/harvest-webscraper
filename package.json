{
  "name": "harvest-webscraper",
  "author": "The StartupBuilder",
  "license": "Apache-2.0",
  "version": "0.0.1",
  "scripts": {
    "test": "ava --verbose",
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/pubsub": "^0.18.0",
    "puppeteer": "^1.8.0",
    "firebase-admin": "~6.0.0",
    "firebase-functions": "^2.0.3"
  },
  "devDependencies": {
    "ava": "^0.25.0",
    "sinon": "^6.3.4",
    "uuid": "^3.3.2",
    "tslint": "~5.8.0",
    "typescript": "~2.8.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kwler/harvest-webscraper.git"
  },
  "private": false
}
